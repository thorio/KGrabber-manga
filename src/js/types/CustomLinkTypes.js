/**
 * Enumerates the link types added by this script
 * @readonly
 * @enum {String}
 */
module.exports = Object.freeze({
	/**
	 * A series of | (pipe) separated URLs that point to images
	 */
	IMAGE_SEQUENCE: "image_sequence",
});
